  **docker pull** - установка образа контейнера.
```bash
docker pull <имя образа>:<тэг> # можно не юзать тег тогда по умолчанию будет последний тэг
```

**docker search** - поиск образа
```bash
docker search <иия образа>
```

**docker run** - запуск образа контейнера создает контейнер
```bash
docker run <имя образа> # Просто запустит контейнер

docker run --name <имя контейнера> <имя образа> # создаст контейнер и сразу запустит его 

docker run -i -t <имя образа> # зайти внутрь контейнера 

docker run -d <имя образа> # запустить образ в отсоидененном режиме.

docker run -p <имя образа> <ip>:<порт компа>:<порт контейнера>

docker run -e <Значение=значение> # позволяет дать переменные окружения
```

**docker ps** - проверить контейнеры
```bash
docker ps -a # проверяет все контейнеры
docker ps -q # берет только id контейнера
docker ps # проверяет только запущенные контейнеры
docker ps -f <критерий> # поиск контейнеров   
```

**docker start** - запустить уже запущенный когда то контейнер
```bash
docker start <имя контейнера> # запустить просто

docker start -i # Покажет вывод запущенного контейнера>
```

**docker stop** - остановить рабочий контейнер
```bash
docker stop <имя контейнера>
```

**docker kill** - принудительно остановить контейнер
```bash
docker kill <имя контейнера>
```

**docker rm** - удалить контейнер
```bash
docker rm <имя контейнера> # удаляем по имени контейнера

docker rm -f <имя контейнера> # 

docker rm <айди контейнера> # удаляем по айди контейнера

docker container prune # удаляет все остановленные контейнеры

docker rmi <имя образа>:<тэг> # удалить образ контейнера. Если не указан тэг то удалиться последний контейнер   
```

**docker rmi** - удалить образ
```bash
docker rmi
```

**docker images** - глянуть образы
```bash
docker images # показать все образы

docker images -a # показать что то скрытое хз
```

**docker history** - глянуть слои из которых состоит образ
```bash
docker history <имя образа> # смотрит все слои
```

**docker build** - создать образ из dockerfile
```bash
docker build <имя контейнера>
docker build -t <имя контейнера>:<тэг>
```

**docker logs** - показать что сейчас выводится в контейнере
```bash
docker logs <имя контейнера>
```

**docker exec** - запустить комманду в контейнере
```bash
docker exec <имя контейнера> <комманда>
```

**docker commit** - сделать образ из существующего контейнера
```bash
docker commit <имя контейнера>
```

**docker builder prune** - очистить кэш
```bash
docker builder prune
docker builder prune -a # удалить больше кэша
docker system prune # удалить все неиспользуемое
```

**docker port** - показывает проброс портов контейнера   
```bash
docker port <имя контейнера>
```

**docker inspect** - показывает детальную информацию о контейнере
```bash
docker inpsect <имя контейнера>
```

**docker rename** -  переименовывает контейнер
```
docker rename <старое имя> <новое имя>
```

**docker logs** - позволяет просмотреть логи контейнера
```bash
docker logs <имя контейнера> 

docker logs -f # позволяет просматривать логи в реальном времени

docker logs --tail <num> # позволяет просмотреть н-ое кол-во строк в логах

docker since <timestamp> # позволяет просмотреть логи с определенного времени

docker stats <контейнер> # позволяет просмотреть CPU,GPU,etc.

```
я типо з
**docker top** - позволяет просмотреть какие процессы крутятся на контейнере
```
docker top <имя контейнера>
```

**docker events**  - честно говоря хз

